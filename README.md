# Streamlit + Snowflake Demo (Dockerized)

A simple Streamlit app that connects to Snowflake, runs queries, and displays results.

## Prerequisites
- Docker Desktop

## Setup
1. Create a local `.env` file (do NOT commit it):
   ```bash
   cp .env.example .env

